<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- White background -->
    <stroke
        android:width="2dp"
        android:color="@android:color/darker_gray" /> <!-- Grey stroke -->
    <corners android:radius="8dp" /> <!-- Rounded corners (optional) -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" /> <!-- Padding inside the button -->
</shape>